{
  "name": "js-better-fs",
  "version": "1.0.0",
  "description": "A better and developer friendly module based on nodejs fs module.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "pretest": "yarn run build",
    "build": "babel src -d lib",
    "prepublish": "yarn run build"
  },
  "author": "Dinsio <dinsio@outlook.com>",
  "license": "GPL-3.0",
  "dependencies": {
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.4",
    "@babel/preset-env": "^7.3.4",
    "jest": "^24.1.0"
  }
}
